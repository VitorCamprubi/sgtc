# --- DB ---
spring.datasource.url=jdbc:mysql://localhost:3306/sgtc?createDatabaseIfNotExist=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=212420
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# --- JPA ---
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.open-in-view=false

# --- CORS (Angular) ---
app.cors.allowed-origins=http://localhost:4200

app.upload.dir=uploads
spring.servlet.multipart.max-file-size=20MB
spring.servlet.multipart.max-request-size=20MB

security.jwt.secret=this-is-a-very-strong-jwt-secret-key-123456
security.jwt.expiration-minutes=60

app.jwt.secret=troque-por-um-segredo-grande-aqui
app.jwt.issuer=sgtc
app.jwt.ttl-seconds=3600
